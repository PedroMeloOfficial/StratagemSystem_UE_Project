# Stratagem Abilities / Rogues

Reserved family — the type asset exists so the category is registered in the system, but no behavior or instances have been built yet.

## `PDA_Stratagem_Rogues`

The typed Data Asset subtype for this family. Currently only extends `PDA_Stratagem_BASE` with no additional family-specific fields — those should be added once it's clear what this family's abilities will actually need.

## Instances

*None yet.* This is the emptiest family in the system — treat it as a placeholder slot for a future Stratagem category (e.g. stealth, deception, or debuff-oriented abilities) rather than something to build against today.
