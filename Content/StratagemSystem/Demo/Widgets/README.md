# Demo / Widgets

| Widget | Purpose |
|---|---|
| `WB_MainMenu_Demo` | The demo's main menu — Play and Quit buttons, nothing more. Loads `Maps/Stratagem_Gym` on Play |

> **Not part of the system.** This exists purely so the project has a presentable start-to-finish flow for a portfolio submission. It has no dependency on `BPC_StratagemSystem` or any Data Asset — it's a plain menu.
